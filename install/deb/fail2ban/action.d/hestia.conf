# Fail2Ban configuration file for skynet

[Definition]

actionstart = /usr/local/skynet/bin/v-add-firewall-chain <name>
actionstop = /usr/local/skynet/bin/v-delete-firewall-chain <name>
actioncheck = iptables -n -L INPUT | grep -q 'fail2ban-<name>[ \t]'
actionban = /usr/local/skynet/bin/v-add-firewall-ban <ip> <name>
actionunban = /usr/local/skynet/bin/v-delete-firewall-ban <ip> <name>
